<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solvd.hospital.dao.ReportDao">


    <resultMap id="reportResultMap" type="Patient">
        <id     property="id"          column="patient_id"/>
        <result property="firstName"   column="first_name"/>
        <result property="lastName"    column="last_name"/>
        <result property="dateOfBirth" column="date_of_birth"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="insured"     column="insured"/>

        <!-- Nested MedicalRecord with its own id for de-duplication -->
        <association property="medicalRecord" javaType="MedicalRecord"
                     resultMap="medicalRecordInReport"/>

        <!-- Single active Admission  -->
        <association property="admission" javaType="Admission"
                     resultMap="admissionInReport"/>
    </resultMap>

    <resultMap id="medicalRecordInReport" type="MedicalRecord">
        <id     property="id"          column="medical_record_id"/>
        <result property="createdDate" column="created_date"/>
        <result property="bloodType"   column="blood_type"/>
        <result property="notes"       column="mr_notes"/>
        <collection property="allergies" ofType="Allergy">
            <id     property="id"   column="allergy_id"/>
            <result property="name" column="allergy_name"/>
        </collection>
    </resultMap>

    <resultMap id="admissionInReport" type="Admission">
        <id     property="id"           column="admission_id"/>
        <result property="admittedAt"   column="admitted_at"/>
        <result property="dischargedAt" column="discharged_at"/>
        <result property="reason"       column="reason"/>
        <association property="payment" javaType="Payment"
                     resultMap="paymentInReport"/>
    </resultMap>

    <resultMap id="paymentInReport" type="Payment">
        <id     property="id"          column="payment_id"/>
        <result property="admissionId" column="admission_id"/>
        <result property="issuedDate"  column="issued_date"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="paidAmount"  column="paid_amount"/>
        <result property="paid"        column="paid"/>
    </resultMap>

    <select id="findPatientsWithAdmissionDetails" resultMap="reportResultMap">
        SELECT
        p.id            AS patient_id,
        p.first_name,
        p.last_name,
        p.date_of_birth,
        p.phone_number,
        p.insured,

        mr.id           AS medical_record_id,
        mr.created_date,
        mr.blood_type,
        mr.notes        AS mr_notes,

        al.id           AS allergy_id,
        al.name         AS allergy_name,

        adm.id          AS admission_id,
        adm.admitted_at,
        adm.discharged_at,
        adm.reason,

        pay.id          AS payment_id,
        pay.issued_date,
        pay.total_amount,
        pay.paid_amount,
        pay.paid

        FROM patients p
        INNER JOIN admissions adm      ON adm.patients_id       = p.id
        LEFT  JOIN payments   pay      ON pay.admissions_id      = adm.id
        LEFT  JOIN medical_records mr  ON mr.patients_id         = p.id
        LEFT  JOIN allergies al        ON al.medical_records_id  = mr.id

        ORDER BY p.id, adm.id, al.id
    </select>

</mapper>